<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://code.alibabatech.com/schema/dubbo
        http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
        
    <!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样 -->
 	<dubbo:application name="${dubbo.application.name}" owner="${dubbo.application.owner}"/>
 	<dubbo:protocol name="${dubbo.protocol.name}" port="${dubbo.protocol.port}" payload="16777216"/>
     
    <dubbo:registry  protocol="${dubbo.registry.protocol}" address="${dubbo.registry.address}" id="omgRegistry"/>
 	<dubbo:registry  protocol="${qk.dubbo.registry.protocol}" address="${qk.dubbo.registry.address}" id="qkRegistry"/>
    <dubbo:reference id="passwordResetApi" interface="cn.uce.omg.sso.api.IPasswordResetApi" version="1.0" retries="0" timeout="6000"/>
</beans>